{
  "name": "oci-focus-to-gcs-hive",
  "version": "1.0.0",
  "description": "Cloud Run job to sync OCI FOCUS billing reports to GCS with Hive partitioning",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.15.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
